using System;

namespace DiceStream {

    public class Parser : IParser {
        public IParsed Parse(string rawStatement) {
            // now we tke the raw string and parse it into a statement
				// how fancy do we want to get about it?
				// if we write a grammar for it and 
				// make a state machine parser
				
				// 














        }
    }

}